#ifndef DEBUGUTILS_H
#define DEBUGUTILS_H

#include <string>

#include "types.h"


std::string rankSeperator(BoardDimension width);
std::string fileLetters(BoardDimension width);

#endif // DEBUGUTILS_H